<%= form_for ([@client, @client.matters.build]) do |f| %>
  <% if @client.errors.any? %>
    <div id = "error-explanation">
	  <h2><%= pluralize(@client.errors.count, "Error") %> Prohibited Matter From Being Saved:</h2>
	  <ul>
      <% @client.errors.full_messages.each do |msg| %>
        <li><%= msg.titleize %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class = "basic-form">
    <%= render "matters/fieldsets", f:f %>
  </div>
  <div class = "basic-form-save-button">
	<%= image_submit_tag("save_button_icon.png", :title => "Save Matter", :alt => "Save Matter") %>
  </div>
<% end %>
