<%= form_for ([@client, @client.vehicles.build]) do |f| %>
  <% if @client.errors.any? %>
    <div id = "errorExplanation">
	  <h2><%= pluralize(@client.errors.count, "Error") %> Prohibited Vehicle From Being Saved:</h2>
	  <ul>
      <% @client.errors.full_messages.each do |msg| %>
        <li><%= msg.titleize.titleize %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class = "basic-form">
    <%= render "vehicles/fieldsets", f:f %>
  </div>
  <div class = "basic-form-save-button">
	<%= image_submit_tag("save_button_icon.png", :title => "Save Vehicle", :alt => "Save Vehicle") %>
  </div>
<% end %>
